Flask==3.0.0
gunicorn==23.0.0

google-auth==2.27.0
google-auth-httplib2==0.2.0
google-auth-oauthlib==1.2.0
google-api-python-client==2.116.0

pymongo==4.6.1

# LangChain core libs (remove explicit langchain-core pin to avoid conflicts)
langchain>=0.2.13,<0.3.0
langchain-community>=0.2.12,<0.3.0
# Gemini integration (replaces OpenAI)
langchain-google-genai
# Optional direct client (sometimes useful for embeddings / future features)
google-generativeai

httplib2==0.22.0
python-dotenv==1.0.1
